apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # fixed UID so dashboard panels resolve the datasource
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
